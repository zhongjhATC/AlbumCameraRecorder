<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zhongjh.demo">

    <!-- 访问网络连接权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 访问网络连接权限bugly所需 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 语音权限，录制声音通过手机或耳机的麦克  -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 修改声音设置信息 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- 某些手机启动SCO音频连接需要此权限 -->
    <uses-permission android:name="android.permission.BROADCAST_STICKY" />
    <!-- 蓝牙录音检测耳机状态 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- Devices running Android 12L (API level 32) or lower  -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Devices running Android 13 (API level 33) or higher -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- To handle the reselection within the app on Android 14 (API level 34) -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- 视频权限 begin -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <!-- 视频权限 end -->

    <application
        android:name="com.zhongjh.demo.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.zhongjh.demo.phone.MainListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.zhongjh.demo.phone.MainSimpleActivity" />

        <activity android:name="com.zhongjh.demo.phone.MainThemeActivity" />

        <activity
            android:name="com.zhongjh.demo.phone.MainActivity"
            android:configChanges="orientation|keyboardHidden|navigation" />

        <activity android:name="com.zhongjh.demo.phone.MainSeeActivity" />
        <activity android:name="com.zhongjh.demo.phone.MainSeeLocalActivity" />

        <activity android:name="com.zhongjh.demo.phone.MainUpperLimitActivity" />

        <activity android:name="com.zhongjh.demo.phone.RecyclerViewActivity" />

        <activity android:name="com.zhongjh.demo.phone.MainCustomCameraViewActivity" />

        <activity android:name="com.zhongjh.demo.phone.MainSuperSimpleActivity" />
        <activity android:name="com.zhongjh.demo.phone.ErrorActivity" />
        <activity android:name="com.zhongjh.demo.phone.custom.MainCustomCameraLayoutActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths_public" />
        </provider>

    </application>

</manifest>