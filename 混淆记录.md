# Android R8精准混淆 Keep保护范围笔记

> 核心原则：**运行时依靠字符串名称查找类/方法的，必须保护；代码直接调用的普通业务代码，可以安全混淆瘦身**

## ✅ 必须添加 `@Keep`（禁止改名、禁止删除）

满足下面任意一条，需要打上注解保护

1. **序列化数据实体**
   > Gson / Moshi / Fastjson 解析的 `data class` / Java Bean  
   > `Parcelable` / `Serializable` 跨页面传递的数据模型  
   > 省事做法,处理所有相关实体包  
   > -keep class com.zhongjh.common.entity.** { *; }

| 搜索目标              | 正则表达式             |
|-------------------|-------------------|
| Serializable 序列化类 | `: Serializable`  |
| Parcelable 序列化类   | `: Parcelable`    |
| Gson字段注解          | `@SerializedName` |
| Fastjson字段注解      | `@JSONField`      |
| Moshi字段注解         | `@Json\(`         |
| 全部data class候选列表  | `^data class`     |

2. **XML声明的自定义View**
   > 继承 `View / ViewGroup / AppCompatXXX`，布局xml中写完整类名的控件  
   > 原理：`LayoutInflater` 通过字符串反射创建控件  
   > 省事做法,处理所有相关实体包 -keep class com.zhongjh.common.widget.** { *; }

3. **JNI Native交互类与方法**
    - 带有 `external fun` / `native` 方法的类
    - C/C++代码硬编码包名、类名调用的上层代码

4. **代码动态反射加载的类**
    - `Class.forName("完整类名")` 硬编码反射
    - 配置/服务端下发类名动态实例化对象

5. **JSBridge 给H5调用的Native类**
    - 带有 `@JavascriptInterface` 方法的宿主类

6. **对外暴露的SDK公开API**
    - 作为AAR提供给外部业务调用、反射使用的类

> 💡 补充：AndroidManifest注册的四大组件(Activity/Service等)，AGP自带默认保护规则，一般不需要手动加`@Keep`

---

## ❗ 不需要添加 `@Keep`（允许R8混淆、压缩）

**同时满足全部条件即可放开混淆：**

1. 所有调用都是代码直接引用（IDE可直接跳转调用位置）
2. 不参与JSON序列化/反序列化
3. 不作为自定义View写在xml布局中
4. 不存在JNI、JSBridge、动态反射逻辑
5. 仅当前模块内部使用，不对外暴露API

> 示例：普通工具类、内部Fragment、业务逻辑类、仅内存临时使用不序列化的局部Model

---

## ❌ 禁止粗放全局保护（避坑红线）

不要直接锁定整个包，混淆优化失效、包体积变大

```proguard
# 错误写法
-keep class com.zhongjh.** { *; }